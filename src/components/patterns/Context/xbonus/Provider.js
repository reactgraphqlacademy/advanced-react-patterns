import React from 'react'

export const Provider = ({ store }) => {
    // TODO to implement the Redux Provider set the store in the context
    // then update the src/components/patterns/HigherOrderComponents/xbonus/connect to read
    // the store from the context
    return null
}